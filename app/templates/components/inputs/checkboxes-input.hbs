{{#each resolvedCollection as |option|}}
  {{component optionComponentName content=option selectedValue=value
    valuePath=optionValuePath stringValuePath=optionStringValuePath labelPath=optionLabelPath
    inputName=inputName inputType=inputType action="selectValue"}}
{{/each}}

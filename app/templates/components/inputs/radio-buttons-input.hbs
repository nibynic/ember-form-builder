{{#each option in collection}}
  {{component optionComponentName content=option selectedValue=value
    valuePath=optionValuePath labelPath=optionLabelPath inputName=inputName
    action="selectValue"}}
{{/each}}

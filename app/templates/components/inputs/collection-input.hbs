{{#each resolvedCollection as |option|}}
  {{component optionComponentName content=option selectedValue=value
    valuePath=optionValuePath stringValuePath=optionStringValuePath
    labelPath=optionLabelPath class=optionClass}}
{{/each}}

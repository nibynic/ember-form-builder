{{component
  (concat "input-wrappers/" wrapper)
  config=config
  inputComponent=(component (concat "inputs/" type "-input") config=config)
  labelComponent=(component "form-builder/label" for=inputElementId isRequired=validations.required text=texts.label)
}}

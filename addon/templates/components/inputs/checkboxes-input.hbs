{{#each resolvedCollection as |option|}}
  {{component optionComponentName content=option selectedValue=value
    disabled=disabled required=normalizedRequired
    inputName=config.name inputType=inputType
    action="selectValue"
  }}
{{/each}}
